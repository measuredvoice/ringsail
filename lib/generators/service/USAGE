Description:
    Explain the generator

Example:
    rails generate service Thing

    It will create a service file int he lib/services folder that has the basic methods
    that a service must implement to be valid.

    It will generate some basic specs that will need to pass, more should be added to test the 
    specifics of the application.
    This will create:
        /lib/services/thing_service.rb
        /spec/services/thing_service_spec.rb
