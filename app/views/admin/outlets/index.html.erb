<div class="row">
  <div class="col-sm-12 col-md-12 main">
    <h1 class="page-header">Social Media Accounts</h1>
      <div class="row placeholders">
        <div class="col-sm-2">
          </br></br></br>
          <ul>
            <li><%= link_to "All", admin_outlets_path %></li>
            <% @services.each do |service| %>
            <li><%= link_to service.longname.titleize, admin_outlets_path(service: service.shortname) %></li>
            <% end %>
          </ul>
        </div>
        <div class="col-sm-10">
          <p>
            <%= paginate @outlets %>
            <text>
              Download: <%= link_to "CSV", admin_outlets_path(format: "csv") %> 
            </text>
          </p>
          <table class="table table-striped">
          <thead>
          <tr>
            <th>Service</th>
            <th>Service URL</th>
            <th>Account</th>
            <th>Organization</th>
            <th>Updated at</th>
            <th>Tags</th>
            <th class = "lastlinks">Actions</th>
          </tr>
          </thead>

          <tbody>
          <% @outlets.each do |outlet| %>
          	<tr class = "tablerow">
          		<td class = "outlet_data"><%= outlet.service %></td>
              <td class = "outlet_data"><%= outlet.service_url %></td>
          		<td class = "outlet_data"><%= outlet.account %></td>
              <td class = "outlet_data"><%= outlet.organization %></td>
              <td class = "outlet_data"><%= outlet.updated_at.strftime("%B %e, %Y %H:%M") %></td>
              <td class = "outlet_data"><%= outlet.tags[1] %></td>
              <td class = "outlet_data lastlinks">
                <ul class = "gly">
                  <li><%= link_to admin_outlet_path(outlet) do %> <i class = "glyphicon glyphicon-info-sign"></i> <% end %></li>
                  <li><%= link_to edit_admin_outlet_path(outlet) do %> <i class = "glyphicon glyphicon-pencil"></i> <% end %></li>
                  <li><%= link_to history_admin_outlet_path(outlet) do %><i class = "glyphicon glyphicon-book"></i> <% end %> </li>
                  <li class = "destroy"><%= link_to admin_outlet_path(outlet), method: :delete, :class => "delete", confirm: 'Are you certain you want to delete this?' do %><i class = "glyphicon glyphicon-remove"></i> <% end %> </li>
                </ul>
              </td>   
          	</tr>
          <% end %>
          </tbody>
          </table>
          <p>
            <text>
              Download: <%= link_to "CSV", admin_outlets_path(format: "csv") %> 
            </text>
            <%= paginate @outlets %>
          </p>
        </div>
    </div>
  </div>
</div>