<%= form_for([:admin,@official_tag]) do |f| %>
    <% if @official_tag.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@official_tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

        <ul>
        <% @official_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :tag_text, "Tag" %> </br>
      <%= f.text_field :tag_text, :class => "form-control formwidth" %>
    </div>
     <div class="field">
      <%= f.label :tag_type, "Type" %> </br>
      <%= f.select :tag_type, OfficialTag.tag_types.keys.to_a.map{ |w| [w.humanize, w]}, {}, :class => "form-control formwidth" %>
    </div>
    <div class="actions">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
<% end %>
