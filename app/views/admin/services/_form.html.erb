<%= form_for(@admin_service, html: {:class => "grid-row usa-form usa-form--large"}) do |f| %>
  <fieldset class="usa-fieldset">
  <% if @admin_service.errors.any? %>
    <div class="usa-alert usa-alert--error" role="alert">
      <div class="usa-alert__body">
        <h3 class="usa-alert__heading">Error: <%= pluralize(@admin_service.errors.count, "error") %> prohibited this service from being saved</h3>
        <ul>
        <% @admin_service.errors.full_messages.each do |message| %>
        <li class="usa-alert__text"><%= message %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :service_url_example, 'URL Example: (May be http://facebook.com/username for facebook)', {class: 'usa-label'} %>
    <%= f.text_field :service_url_example, :class => "usa-input" %>
  </div>
  <div class="field">
    <%= f.label :handles_regex_eval, 'Service host match regexp: (Should look like /facebook.com$/ for facebook)', {class: 'usa-label'} %>
    <%= f.text_field :handles_regex_eval, :class => "usa-input" %>
  </div>
  <div class="field">
    <%= f.label :shortname, 'Short name: (No spaces, no special characters)', {class: 'usa-label'} %>
    <%= f.text_field :shortname, :class => "usa-input" %>
  </div>
  <div class="field">
    <%= f.label :longname, 'Long name: (Display name)', {class: 'usa-label'} %>
    <%= f.text_field :longname, :class => "usa-input" %>
  </div>
  <!-- <div class="field">
    <%= f.label :display_name_eval, 'Display name template' %><br>
    <%= f.text_field :display_name_eval, :class => "form-control formwidth" %>
  </div>
  <div class="field">
    <%= f.label :account_matchers_eval, 'Account id regexp matchers' %><br>
    <%= f.fields_for :account_matchers_eval do |o| %>
        &nbsp;&nbsp;<%= o.label :nil, 'Reject all regexp, rejects all paths that match this regex' %>
        <%= o.text_field :nil, :value => @admin_service.account_matchers_eval[:nil],
                         :class => "form-control formwidth" %>

        &nbsp;&nbsp;<%= o.label :host, 'Host match regexp, matches the host portion of the url' %>
        <%= o.text_field :host, :value => @admin_service.account_matchers_eval[:host],
                         :class => "form-control formwidth" %>

        &nbsp;&nbsp;<%= o.label :path, 'Path match regexp, matches the account path of the url' %>
        <%= o.text_field :path, :value => @admin_service.account_matchers_eval[:path],
                         :class => "form-control formwidth" %>

        &nbsp;&nbsp;<%= o.label :fragment, 'Fragment/path match regexp, matches fragments of the path' %>
        <%= o.text_field :fragment, :value => @admin_service.account_matchers_eval[:fragment],
                         :class => "form-control formwidth" %>

        &nbsp;&nbsp;<%= o.label :conditional, 'Conditional regexps' %><br />
        <%= o.fields_for :conditional do |c| %>

          &nbsp;&nbsp;&nbsp;&nbsp;<%= c.label :if, '"If" regexp, a conditional regex match for then/else behaviors' %>
          <%= c.text_field :if, :value => @admin_service.account_matchers_eval[:conditional].try(:[], :if),
                           :class => "form-control formwidth" %>

          &nbsp;&nbsp;&nbsp;&nbsp;<%= c.label :then, '"Then" regexp, regex to use if "if" regex matches' %>
          <%= c.text_field :then, :value => @admin_service.account_matchers_eval[:conditional].try(:[], :then),
                           :class => "form-control formwidth" %>

          &nbsp;&nbsp;&nbsp;&nbsp;<%= c.label :else, '"Else" regexp, regex to use if "if" regex fails' %>
          <%= c.text_field :else, :value => @admin_service.account_matchers_eval[:conditional].try(:[], :else),
                           :class => "form-control formwidth" %>
        <% end %>
    <% end %> 
  </div>-->
 
  <!-- <div class="field">
    <%= f.label :service_url_canonical_eval, 'Service url canonical template' %><br>
    <%= f.text_field :service_url_canonical_eval, :class => "form-control formwidth" %>
  </div> -->
  <div class="actions">
    <%= f.submit :class => "usa-button" %>
  </div>
</fieldset>
<% end %>
