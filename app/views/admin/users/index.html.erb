<div class="col-sm-12 col-md-12 main">
  <h1 class="page-header">Outlets</h1>

  <div class="row placeholders">
  	<table class="table-responsible striped">
  		<tr>
  			<th>Email</th>
  			<th>Actions</th>
  		</tr>
  		<% @users.each do |user| %>
  			<tr>
  				<td><%= user.email %></td>
  				<td><%= link_to "Edit", edit_admin_user_path(user)%></td>
  			</tr>
  		<% end %>
  	</table>
  </div>
</div>